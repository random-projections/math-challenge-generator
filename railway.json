{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install && npm run build",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["python312", "nodejs", "python312Packages.pip"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "python3 -m pip install -r backend/requirements.txt && cd backend && uvicorn challenge_server:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 